package DataTypes;

/**
 * Used to calculate the outcome of a collision between two subclasses of
 * Entity.
 */
public enum CollisionType {
    BOMB,
    LOOT,
    CLOCK,
    LEVER,
    GATE,
    DOOR,
    ASSASSIN,
    THIEF,
    PLAYER,
}
